{% macro pageFooterCta(exhibition) %}
<section class="rounded-[32px] bg-brand-dark px-10 py-14 text-brand-light shadow-card">
  <div class="flex flex-col gap-8 lg:flex-row lg:items-center lg:justify-between">
    <div class="flex-1">
      <h2 class="text-3xl font-semibold md:text-4xl">{{ exhibition.title }} を体験しましょう</h2>
      <p class="mt-4 max-w-xl text-brand-light/80">
        最新の展示情報とチケット情報を確認し、訪問計画を立ててください。
      </p>
    </div>
    <div class="flex flex-col gap-4 lg:items-end">
      <a class="btn-primary px-8" href="{{ exhibition.ctaUrl }}">{{ exhibition.ctaLabel }}</a>
      <a
        class="inline-flex items-center gap-2 text-sm font-semibold text-brand"
        href="{{ exhibition.overviewUrl }}"
        target="_blank"
        rel="noreferrer"
      >
        公式サイトでもっと詳しく見る
        <span aria-hidden="true">→</span>
      </a>
    </div>
  </div>
</section>
{% endmacro %}
