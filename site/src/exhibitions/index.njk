---
layout: layouts/base.njk
title: 展示会一覧
permalink: /exhibitions/index.html
pageDescription: 最新の展示会情報を一覧で確認できます。
ogImage: https://cdn.example.com/placeholders/exhibition-og.jpg
---

{% set exhibitionsList = exhibitions.list or [] %}

<section class="exhibitions-index" aria-labelledby="exhibitions-heading">
  <div class="container">
    <h1 id="exhibitions-heading">展示会一覧</h1>
    <p class="intro">最新の展示会情報を一覧でご覧いただけます。各展示会の詳細ページでは見どころや開催背景をご確認ください。</p>

    <div class="exhibitions-grid">
      {% for exhibition in exhibitionsList %}
        <article class="exhibition-card" aria-labelledby="exhibition-{{ exhibition.id }}">
          <img
            src="{{ exhibition.heroImage.src }}"
            alt="{{ exhibition.heroImage.alt }}"
            class="exhibition-card__image"
            loading="lazy"
          />
          <div class="exhibition-card__body">
            <h2 id="exhibition-{{ exhibition.id }}" class="exhibition-card__title">
              <a href="/exhibitions/{{ exhibition.id }}/">{{ exhibition.title }}</a>
            </h2>
            {% if exhibition.period.display %}
              <p class="exhibition-card__period">{{ exhibition.period.display }}</p>
            {% endif %}
            <p class="exhibition-card__venue">{{ exhibition.venue }}</p>
            <p class="exhibition-card__summary">{{ exhibition.summary }}</p>
            <ul class="exhibition-card__links" data-related-links="pending-label-spec">
            {# TODO: Clarify related link labeling order once specification is confirmed #}
              <li><a href="{{ exhibition.officialUrl }}" target="_blank" rel="noopener">公式サイトを開く</a></li>
              {% if exhibition.relatedUrls and exhibition.relatedUrls | length > 0 %}
                {% for link in exhibition.relatedUrls %}
                  <li><a href="{{ link.url }}" target="_blank" rel="noopener">{{ link.label }}</a></li>
                {% endfor %}
              {% endif %}
            </ul>
          </div>
        </article>
      {% endfor %}

      {% if exhibitionsList | length == 0 %}
        <p class="exhibition-card__empty">現在表示できる展示会はありません。</p>
      {% endif %}
    </div>
  </div>
</section>
