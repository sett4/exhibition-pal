---
layout: layouts/base.njk
title: 展示会一覧
permalink: /exhibitions/index.html
pageDescription: 最新の展示会情報を一覧で確認できます。
ogImage: https://cdn.example.com/placeholders/exhibition-og.jpg
bodyClass: exhibitions-body
---

{% from "components/exhibition-card.njk" import render as exhibitionCard %}

{% set exhibitionsList = exhibitions.list or [] %}
{% set featured = exhibitionsList | first %}
{% set rest = exhibitionsList | slice(1) %}

{% block head %}
  {% if featured and featured.heroImage and featured.heroImage.src %}
    <link rel="preload" as="image" href="{{ featured.heroImage.src }}" type="image/jpeg" />
  {% endif %}
{% endblock %}

<section class="exhibitions-index" aria-labelledby="exhibitions-heading">
  <div class="exhibitions-container">
    <header class="exhibitions-index__header">
      <p class="exhibitions-index__kicker">Curated Highlights</p>
      <h1 id="exhibitions-heading" class="exhibitions-index__title">展示会一覧</h1>
      <p class="exhibitions-index__intro">
        最新の展示会情報を一覧でご覧いただけます。各展示会の詳細ページでは見どころや開催背景、収録作品のスライダーを確認できます。
      </p>
    </header>

    <div class="exhibitions-grid" data-test="exhibitions-grid">
      {# {% if featured %}
        {{ exhibitionCard(featured, 'featured') | safe }}
      {% endif %}
 #}
      {% for exhibition in exhibitionsList %}
        {{ exhibitionCard(exhibition) | safe }}
      {% endfor %}

      {# {% if exhibitionsList | length == 0 %}
        <p class="exhibition-card__empty">現在表示できる展示会はありません。</p>
      {% endif %} #}
    </div>
  </div>
</section>
